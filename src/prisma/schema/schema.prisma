// @ts-eslint-ignore-file
generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
  output        = "../generated"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")?ssl-mode=REQUIRED
}
